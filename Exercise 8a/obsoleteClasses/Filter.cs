using System;
using System.Collections.Generic;
using System.Text;

namespace SecondProject.Exercise_8a
{
    class Filter
    {
        //public void filter(string field, string value)
        //{
            //foreach (ContactItem element in UserContactsList.contactList)
            //{
                //foreach (KeyValuePair<string, string> contactData in element.userData)
                //{
                    //if (contactData.Key.Equals(field) && contactData.Value.Equals(value))
                    //{
                        //foreach (var entry in element.userData)
                            //Console.WriteLine("[{0} {1}]", entry.Key, entry.Value);
                    //}
                //}
            //}
        //}
    }
}
